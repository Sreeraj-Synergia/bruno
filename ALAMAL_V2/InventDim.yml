info:
  name: InventDim
  type: http
  seq: 10

http:
  method: GET
  url: "{{DynamicsURL}}/data/InventDimCDSEntities"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
