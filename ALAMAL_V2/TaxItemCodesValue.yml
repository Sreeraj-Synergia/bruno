info:
  name: TaxItemCodesValue
  type: http
  seq: 51

http:
  method: GET
  url: "{{DynamicsURL}}/data/TaxCodeValuesV2"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
