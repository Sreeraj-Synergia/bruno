info:
  name: DirPartyTableBiEntities
  type: http
  seq: 72

http:
  method: GET
  url: "{{DynamicsURL}}/data/DirPartyTableBiEntities"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
