info:
  name: Item Master Data
  type: http
  seq: 2

http:
  method: GET
  url: "{{DynamicsURL}}/data/ReleasedProductsV2?$filter=ModifiedDate ge 2026-12-01"
  params:
    - name: $filter
      value: ModifiedDate ge 2026-12-01
      type: query
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
