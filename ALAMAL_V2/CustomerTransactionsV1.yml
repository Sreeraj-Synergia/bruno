info:
  name: CustomerTransactionsV1
  type: http
  seq: 71

http:
  method: GET
  url: "{{DynamicsURL}}/data/ProductCategoryAssignments"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
