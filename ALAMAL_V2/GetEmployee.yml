info:
  name: GetEmployee
  type: http
  seq: 52

http:
  method: GET
  url: "{{DynamicsURL}}/data/EmployeesV2?$filter=ProfessionalSuffix eq 'SFA'"
  params:
    - name: $filter
      value: ProfessionalSuffix eq 'SFA'
      type: query
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
