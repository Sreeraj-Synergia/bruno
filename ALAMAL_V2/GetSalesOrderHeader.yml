info:
  name: GetSalesOrderHeader
  type: http
  seq: 28

http:
  method: GET
  url: "{{DynamicsURL}}/data/SalesOrderHeadersV2"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
