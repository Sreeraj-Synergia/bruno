info:
  name: Item Master Data FP Copy
  type: http
  seq: 5

http:
  method: GET
  url: "{{DynamicsURL}}/data/ReleasedProductsV2?$filter=ItemNumber eq '3104001'"
  params:
    - name: $filter
      value: ItemNumber eq '3104001'
      type: query
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
