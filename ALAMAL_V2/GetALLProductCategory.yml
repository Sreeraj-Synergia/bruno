info:
  name: GetALLProductCategory
  type: http
  seq: 49

http:
  method: GET
  url: "{{DynamicsURL}}/data/ProductCategories"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
