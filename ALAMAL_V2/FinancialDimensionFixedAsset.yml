info:
  name: FinancialDimensionFixedAsset
  type: http
  seq: 14

http:
  method: GET
  url: "{{DynamicsURL}}/data/FinancialDimensionValues?$filter=FinancialDimension eq 'FixedAsset'"
  params:
    - name: $filter
      value: FinancialDimension eq 'FixedAsset'
      type: query
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
