info:
  name: Item Master Data FP
  type: http
  seq: 4

http:
  method: GET
  url: "{{DynamicsURL}}/data/ReleasedProductsV2"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
