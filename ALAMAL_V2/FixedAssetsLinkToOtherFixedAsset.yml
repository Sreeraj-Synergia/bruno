info:
  name: FixedAssetsLinkToOtherFixedAsset
  type: http
  seq: 57

http:
  method: GET
  url: "{{DynamicsURL}}/data/FixedAssetBooksV2?$filter=FixedAssetGroupId eq 'VEHI'"
  params:
    - name: $filter
      value: FixedAssetGroupId eq 'VEHI'
      type: query
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
