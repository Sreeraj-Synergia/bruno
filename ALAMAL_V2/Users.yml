info:
  name: Users
  type: http
  seq: 22

http:
  method: GET
  url: "{{DynamicsURL}}/data/SystemUsers"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
