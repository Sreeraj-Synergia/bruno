info:
  name: TaxItemGroup
  type: http
  seq: 50

http:
  method: GET
  url: "{{DynamicsURL}}/data/TaxItemGroups"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
