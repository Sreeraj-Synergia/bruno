info:
  name: ItemBatch
  type: http
  seq: 7

http:
  method: GET
  url: "{{DynamicsURL}}/data/$metadata"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
