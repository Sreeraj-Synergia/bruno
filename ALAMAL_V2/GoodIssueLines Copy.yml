info:
  name: GoodIssueLines Copy
  type: http
  seq: 45

http:
  method: GET
  url: "{{DynamicsURL}}/data/InventoryTransferJournalEntries"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
