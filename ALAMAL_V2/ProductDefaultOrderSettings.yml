info:
  name: ProductDefaultOrderSettings
  type: http
  seq: 3

http:
  method: GET
  url: "{{DynamicsURL}}/data/ProductDefaultOrderSettings"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
