info:
  name: CustomerPostalAddresses Copy
  type: http
  seq: 12

http:
  method: GET
  url: "{{DynamicsURL}}/data/CustomerPostalAddresses"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
