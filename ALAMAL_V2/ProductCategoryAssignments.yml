info:
  name: ProductCategoryAssignments
  type: http
  seq: 70

http:
  method: GET
  url: "{{DynamicsURL}}/data/ProductCategoryAssignments"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
