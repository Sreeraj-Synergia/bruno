info:
  name: WarehousesOnHand
  type: http
  seq: 69

http:
  method: GET
  url: "{{DynamicsURL}}/data/WarehousesOnHandV2"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
