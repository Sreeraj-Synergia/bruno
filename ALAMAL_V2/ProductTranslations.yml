info:
  name: ProductTranslations
  type: http
  seq: 6

http:
  method: GET
  url: "{{DynamicsURL}}/data/ProductTranslations?$filter=ProductNumber eq '310410'"
  params:
    - name: $filter
      value: ProductNumber eq '310410'
      type: query
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
