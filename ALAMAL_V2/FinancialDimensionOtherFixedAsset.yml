info:
  name: FinancialDimensionOtherFixedAsset
  type: http
  seq: 15

http:
  method: GET
  url: "{{DynamicsURL}}/data/FinancialDimensionValues?$filter=FinancialDimension eq 'OtherFixedAsset'"
  params:
    - name: $filter
      value: FinancialDimension eq 'OtherFixedAsset'
      type: query
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
