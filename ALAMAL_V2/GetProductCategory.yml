info:
  name: GetProductCategory
  type: http
  seq: 48

http:
  method: GET
  url: "{{DynamicsURL}}/data/ProductCategoryAssignments?$filter=ProductNumber eq 'RAW-PM-006'"
  params:
    - name: $filter
      value: ProductNumber eq 'RAW-PM-006'
      type: query
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
