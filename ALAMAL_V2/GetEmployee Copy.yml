info:
  name: GetEmployee Copy
  type: http
  seq: 53

http:
  method: GET
  url: "{{DynamicsURL}}/data/EmployeesV2"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
