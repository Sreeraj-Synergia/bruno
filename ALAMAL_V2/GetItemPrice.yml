info:
  name: GetItemPrice
  type: http
  seq: 23

http:
  method: GET
  url: "{{DynamicsURL}}/data/SalesPriceAgreements"
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
