info:
  name: InventTransOrg
  type: http
  seq: 8

http:
  method: GET
  url: "{{DynamicsURL}}/data/InventTransOriginBiEntities?$filter=ReferenceId eq 'WINIT-000101'"
  params:
    - name: $filter
      value: ReferenceId eq 'WINIT-000101'
      type: query
  auth:
    type: bearer
    token: "{{Token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
