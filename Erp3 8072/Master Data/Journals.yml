info:
  name: Journals
  type: http
  seq: 1

http:
  method: POST
  url: "{{Erp3-Local-8072}}/web/get_payment_journals"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
