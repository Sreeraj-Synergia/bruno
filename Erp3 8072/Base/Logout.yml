info:
  name: Logout
  type: http
  seq: 4

http:
  method: POST
  url: "{{Erp3-Local-8072}}/web/session/mobile_logout"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
