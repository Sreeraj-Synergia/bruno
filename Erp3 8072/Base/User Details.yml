info:
  name: User Details
  type: http
  seq: 4

http:
  method: POST
  url: "{{Erp3-Local-8072}}/web/user_details"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
