info:
  name: Change Password
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8072/web/session/change_user_password
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
