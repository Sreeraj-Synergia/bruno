info:
  name: Customer Invoices
  type: http
  seq: 4

http:
  method: POST
  url: "{{Erp3-Local-8072}}/web/get_user_invoice_details"
  body:
    type: json
    data: |-
      {
          "params": {
              "outstanding": false,
              "page": 0,
              "data": {
                  "invoice_id": 43106
              }
          }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
